# HomeStream

A simple homemade streaming service built with Python and FastAPI.

## Setup

1. Create virtual environment
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `uvicorn app.main:app --reload`
